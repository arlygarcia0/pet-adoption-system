//import all Pets package
import Pets.*;

public class adoptionCenter {
    //Attribute
    Pet[] inShelter; //An array showing the pets that are in the shelter
    Pet[] adopted; //an array showing the pets that have been adopted
    int size;

    public void insert (Pet name, int index) {

    }

    //Constructor
    public adoptionCenter () {
        inShelter = new Pet[size];
    }
}